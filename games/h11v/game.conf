title = H11V
description = A pocket voxel world that an algorithm slowly rewrites. A first-person player and three bots share one 128x128 island; H11 mutates the blocks and the creatures together.
author = ichMaster
# The engine floor is NOT declared here. `min_luanti_version` sat on this line
# and no engine ever read it — lua_api.md's list of game.conf keys has no version
# key at all. It is checked at load instead, in mods/h11_world/init.lua.
