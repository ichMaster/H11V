# H11V — Full code, design & visualization review (at v0.8.0)

**Date:** 2026-09-14 · **Scope:** the repository at tag `0.8.0` (commit `4130b54`) — game Lua, shell tooling, Python gates, all specification documents, the installed asset pack, and the on-device rendering evidence. **`codegen/` (pipeline instrumentation) is out of scope for this report.**
**Method:** adversarial multi-agent review. Seven independent reviewers over disjoint scopes (lua-world, lua-player, sh-deploy, sh-gates, py-tools, design, viz); **every finding was then handed to an independent verifier instructed to refute it** — re-reading the cited file, checking claims against the engine's own `lua_api.md`, reproducing failures where possible, and checking `docs/decisions.md` so recorded, deliberate tradeoffs are not re-reported as bugs. A completeness critic then searched for what the reviewers missed; its most serious claims were re-verified by hand.
**Outcome:** 69 findings raised in scope, **1 refuted**, 68 confirmed; after cross-dimension dedup, **65 distinct findings**, plus **6 from the completeness pass** = **71 findings: 9 HIGH · 27 MEDIUM · 35 LOW.** Findings marked *(accepted)* touch a decision recorded in `docs/decisions.md` but add a consequence the decision did not consider.

No fixes were applied by this review. Every finding below is open.

---

## Executive summary

1. **The documentation drifted much further behind the code than the code drifted behind anything.** Six of nine HIGH findings are spec lines that would actively misdirect the next contributor — starting with `CLAUDE.md` still declaring the repo *"specification-only — no code exists yet"* against 64 commits and a released, device-tested game, and prescribing a video driver (`ogles2`) that makes the device build refuse to start. `docs/decisions.md`, by contrast, matched the code everywhere the reviewers checked.
2. **One real credential-exposure path exists in scope.** The deploy's `sshpass -p` fallback puts the device password into `ps`-visible argv for the whole transfer, and the unquoted expansion silently breaks passwords containing a space.
3. **The gates are mostly sound, and several measure something other than what they claim.** `run_local.sh` crashes on every documented flag; `check_assets.py --pack` audits the retired v0 pack, so the colony delivery of record cannot be gate-audited at all; `check_lua.sh` parses with Lua 5.4 syntax the engine's LuaJIT rejects; the post-launch llvmpipe confirmation in the deploy is dead code.
4. **The visualization layer is in good shape where it was measured, and the audit itself has three unrecorded gaps.** The shipped pack's ladder steps, the melt tint, the scanner extrusion contract and the install pipeline all re-verified exactly. But the §4.1 ladder *order* was inverted by the delivery while three documents claim compliance; two Rule-2 hue collisions and a fourth Rule-5 pair were never audited; and on the live device the engine's touch overlay draws a zoom magnifier and a raw `Aux1` label mid-screen — tapping the magnifier turns the camera instead of zooming.

The one **refuted** finding is worth keeping: a reviewer claimed the worldgen probe races future ABMs; the verifier proved emerged-but-playerless blocks are never *active* in Luanti, so ABMs cannot fire during the gate. The adversarial pass exists for exactly this.

---

## Summary table — HIGH and MEDIUM

Recommendation: **FIX NOW** = small, self-contained, high-value; **DEFER →** names the home. Status is left open for a follow-up fix pass (`/review-and-fix-issues` or manual).

| # | Sev | Area | Finding | Anchor | Recommendation |
|---|-----|------|---------|--------|----------------|
| H1 | 🔴 | deploy | Device password in `ps`-visible rsync argv via `RSYNC_RSH` (sshpass fallback) | `tools/deploy_to_term35.sh:119` | FIX NOW |
| H2 | 🔴 | gates | `run_local.sh` forwards its own parsed flags to Luanti → fatal on every documented option | `tools/run_local.sh:94` | FIX NOW |
| H3 | 🔴 | gates | `check_assets.py --pack` audits the retired v0 pack; colony delivery un-auditable (found independently by 3 dimensions) | `tools/check_assets.py:28` | FIX NOW |
| H4 | 🔴 | docs | `CLAUDE.md` "Current state": *specification-only, no code, single commit* — against a released v0.8.0 | `CLAUDE.md:7` | FIX NOW |
| H5 | 🔴 | docs | `CLAUDE.md` device constraints prescribe `ogles2` (device build refuses to start), font 20 / `gui_scaling` 1.4 (reintroduces two fixed defects) | `CLAUDE.md:97` | FIX NOW |
| H6 | 🔴 | docs | `CLAUDE.md` layout/milestones: dangling "M0 spec §7/§8" refs, "Luanti not installed", missing `h11_build`, 8 block types | `CLAUDE.md:73` | FIX NOW |
| H7 | 🔴 | docs | `ARCHITECTURE §Components`: nine nodes with retired ids — the very list v1's rules table will be written against | `specification/ARCHITECTURE.md:53` | FIX NOW |
| H8 | 🔴 | docs | `ARCHITECTURE §Assets`: still "ships at 16×16" from `art/h11v/` — both reversed by recorded decisions | `specification/ARCHITECTURE.md:241` | FIX NOW |
| H9 | 🔴 | viz | Shipped pack inverts §4.1 Rule 1's ladder **order** (hull brightest, lithic darkest); §11, ROADMAP and nodes.lua all assert compliance | `specification/ART-COLONY.md:370` | FIX NOW (record the deviation) |
| M1 | 🟠 | lua | `mgv7_spflags` never set: stock v7 mountains & river channels remain enabled outside the measured window | `games/h11v/mods/h11_world/mapgen.lua:83` | FIX NOW |
| M2 | 🟠 | lua | `game.conf` `min_luanti_version` is not a key any engine reads — version pin silently inert | `games/h11v/game.conf:4` | FIX NOW |
| M3 | 🟠 | lua | `settle()` stands the player on top of spire crowns (~24% of spawn candidates); the descend-under-tree branch can never fire | `games/h11v/mods/h11_world/player.lua:147` | FIX NOW |
| M4 | 🟠 | deploy | Post-launch llvmpipe confirmation is dead code: `debug.txt` never contains a renderer line at default log level | `tools/deploy_to_term35.sh:309` | FIX NOW |
| M5 | 🟠 | deploy | `gpu_preflight` can PASS on EGL evidence alone when the GLX probe silently yields nothing | `tools/gpu_preflight.sh:74` | FIX NOW |
| M6 | 🟠 | deploy | `measure_device.sh` discards all deploy output — silencing the renderer warning during the run that mints numbers | `tools/measure_device.sh:71` | FIX NOW |
| M7 | 🟠 | deploy | Deploy swaps the config without verifying the engine died after SIGKILL escalation (slow-SD D-state gap) | `tools/deploy_to_term35.sh:248` | FIX NOW |
| M8 | 🟠 | deploy | `gpu_preflight` header and `--help` still teach the abandoned EGL-first rationale its code reversed | `tools/gpu_preflight.sh:15` | FIX NOW |
| M9 | 🟠 | deploy | Second Sway-menu launch restores desktop scale under the still-running game (no liveness check on STATE) | `tools/run_on_pi.sh:64` | FIX NOW |
| M10 | 🟠 | gates | `check_lua.sh` prefers Lua 5.4 `luac`: syntax LuaJIT rejects gets a green gate | `tools/check_lua.sh:15` | FIX NOW |
| M11 | 🟠 | gates | `install_assets.sh --res=16` can exit 0 without downscaling anything (unchecked pipeline steps) | `tools/install_assets.sh:50` | FIX NOW |
| M12 | 🟠 | gates | *(accepted)* `MIN_TREES=4` silently demands ~64 growths — 3.2× the DoD; one density drop from red | `tools/test_worldgen.sh:176` | FIX NOW (`MIN_TREES=1`) |
| M13 | 🟠 | gates | `mktemp -d -t <prefix>` is BSD-only: the worldgen gate dies on the Pi / any GNU box | `tools/test_worldgen.sh:58` | FIX NOW |
| M14 | 🟠 | gates | `ln -sfn` cannot replace a pre-existing real dir: `run_local` can silently run a stale game copy | `tools/run_local.sh:56` | FIX NOW |
| M15 | 🟠 | py | Seam metric ignores alpha and self-inflates its threshold — `h11_bloom`'s tiling check near-vacuous | `tools/check_assets.py:231` | DEFER → next art re-delivery |
| M16 | 🟠 | py | Alpha-regime classifier fully bypassed by a flattened RGB re-export (binary textures would ship opaque, gate green) | `tools/check_assets.py:269` | FIX NOW |
| M17 | 🟠 | design | ROADMAP v1.4 replays missed cycles — the architecture (and CLAUDE.md) settle the opposite | `specification/ROADMAP.md:224` | FIX NOW (before v1 issue generation) |
| M18 | 🟠 | design | `h11_build` ("from v2") is scheduled in **no** roadmap phase — the second model seam has no home | `specification/ROADMAP.md:229` | FIX NOW |
| M19 | 🟠 | design | The plan→`h11_build` transport is unspecified: second seam has no endpoint, wake condition, or budget | `specification/ARCHITECTURE.md:117` | DEFER → v2 planning, record in decisions.md |
| M20 | 🟠 | design | *(accepted)* v4 anchor breaks the frontier function's stated purity — anchor history is an unstated persistent input | `specification/ARCHITECTURE.md:175` | DEFER → v4 design, note now |
| M21 | 🟠 | design | SDLC.md describes `/ship-solution` as the opposite of what the skill is | `specification/SDLC.md:32` | FIX NOW |
| M22 | 🟠 | viz | Two Rule-2 hue collisions absent from §11's "seven, none blocking": `regolith_side↔prefab` (ΔL\*5.1, ΔH50°, ΔC5.3 — no chroma lever) and `lithic_top↔fines` | `specification/ART-COLONY.md:375` | FIX NOW (adjudicate & record) |
| M23 | 🟠 | viz | Rule 5 audited "3 of 3" but the catalogue ships **four** top/side pairs; lithic's is +2.9 — thinner than the +2.6 the audit itself rejected | `specification/ART-COLONY.md:371` | FIX NOW (measure, accept or brighten) |
| M24 | 🟠 | viz | `gamepad.conf` asserts `zoom_fov=fov` neutralizes zoom — decisions.md and turn.lua record the measured opposite | `tools/device/gamepad.conf:57` | FIX NOW |
| M25 | 🟠 | viz | Touch overlay draws a magnifier + raw `Aux1` label mid-screen; tapping the magnifier **turns the camera**, and both eat look/dig taps | `tools/device/gamepad.conf:50` | FIX NOW (`zoom_fov = 0` property) |
| M26 | 🟠 | cross | `specification/implementation/` stops at v0.3 while tags run to 0.8.0 — `harden-findings` sweeps those files and is therefore structurally blind (hand-verified) | `specification/implementation/` | DEFER → process decision, record it |
| M27 | 🟠 | cross | The world-size debt "owed to v1" (world still unbounded; VISION's pillar unimplemented) has no home in any roadmap phase | `docs/decisions.md:185` | FIX NOW (add to v1 tasks) |

---

## Detailed findings

### A. Security & credentials

**H1 — the device password is `ps`-visible during every fallback transfer.** `deploy_to_term35.sh:119` builds `RSYNC_RSH="sshpass -p $DEV_PASS ssh …"`; rsync receives it via `-e` in its own argv, which rsync never masks — so for the whole multi-second transfer, `ps aux` shows the password in clear text (the plain `sshpass -p` uses at lines 100/117 have the same exposure as a startup race). The unquoted expansion also means a password containing a space silently truncates, and the remainder becomes a remote command word — a baffling auth failure. This path runs only when key auth fails *and* sshpass is installed — but line 127 explicitly tells the user to install it. **Fix:** `sshpass -e` in all three paths (password via the `SSHPASS` env var exported only to the child), including `--setup-key`; this also fixes the word-splitting breakage.

### B. Game code (Lua)

**M1 — stock v7 mountains and rivers are still on.** Nothing sets `mgv7_spflags` (engine default: `mountains,ridges,nofloatlands,caverns`); the mountain pass (`mgv7_np_mount_height`: offset 256, scale 112) and ridge river-carving run *independently* of the three pinned noises, so the "one dial" claim in `mapgen.lua` holds only inside the measured window. The world is deliberately unbounded (recorded), so a player walking out — or any seed change — can meet stock mountains above the biome's `y_max=200` rendering as bare lithic, and river canyons beside a comment that says "the pocket world has… not rivers". **Fix:** `core.set_mapgen_setting("mgv7_spflags", "nomountains,noridges,nofloatlands,nocaverns", true)` beside the `mg_flags` override; record the choice.

**M2 — the version pin is inert.** `min_luanti_version` is not among the game.conf keys any engine reads (verified against the full 5.17 bundle; ContentDB's key is spelled `min_minetest_version`). An older engine shows no version message, only the first Lua error. Enforce in `init.lua` via a `core.features` check, or drop the line.

**M3 — spawn on crowns.** `settle()`'s descent stops at the first solid node with two air above — for any column under a spire growth that is the *top of the bloom crown*, so the player spawns standing on the canopy; the keep-descending branch only fires for interior gaps. At density 0.010 with 5×5 crowns, roughly a quarter of candidates are affected; `get_spawn_level` is terrain-only, so the spiral cannot avoid them. Benign (on top, not inside) but the comments describe behaviour the code does not have. **Fix:** skip nodes in groups `leaves`/`tree` during the descent.

**LOW (Lua):** L1 `TREE_DENSITY` unclamped — `≥10` flips the engine into complete-coverage placement, `≤0`/garbage silently deletes all growths *(accepted decision, unconsidered consequence)* · L2 `liquid_range=7` only on the flowing row — split-brain liquid pair · L3 `leafdecay=3` is a minetest_game convention no engine code implements: crowns float forever, the group reads as a promise *(accepted)* · L4 "mandatory" lava/cobble aliases are actually optional-and-deprecated per lua_api · L5 `init.lua` header omits `music`/`turn` from the documented load order · L6 `h11_world.NODES` export comment names a consumer that doesn't exist · L7 `/music` toggles a global flag but stops only the caller; not persisted · L8 a player who rejoins during the pending spawn-emerge is never settled (close over the name, not the ObjectRef) · L9 `set_properties({hp_max=20})` is a no-op with a misleading comment · L10 turn step unclamped — a 2 s server stall spins the camera ~250° (`math.min(dtime, 0.1)`) · L11 FOV pin reads the setting once at load; `set_fov(1, true)` pins to the client's own FOV · L12 `WATER_LEVEL=6` duplicates mapgen's value with no shared constant.

### C. Tooling & gates (shell, Python)

**H2 — `run_local.sh` crashes on every documented flag.** The option loop parses `--profile/--fullscreen/--world/--fresh` but never consumes them; line 94 passes the untouched `"$@"` to `exec "$LUANTI"`, and the engine (verified against the local binary; same parser on 5.10) aborts on any unknown parameter with an option dump that blames the engine. Only the bare invocation works. **Fix:** drop `"$@"` from the exec.

**H3 — the pack-audit mode can only audit the wrong pack.** `PACK = specification/art/h11v` (line 28) while the filename tables were retargeted to colony names and `install_assets.sh` installs from `specification/art/colony`. Running `--pack` today: exit 1 with 17 spurious "missing" findings. Three dimensions found this independently; a re-delivery from the designer can never be gate-audited. **Fix:** repoint `PACK`, the `--pack` help string, the module docstring, and `install_assets.sh`'s line-8 comment.

**M4/M5/M6 — the renderer-verification chain has three gaps that compound.** The deploy's post-launch llvmpipe grep never fires (no repo file sets `debug_log_level`; at the default level `debug.txt` contains no renderer line — the "confirmation" is dead code); the preflight can PASS on EGL evidence alone when `glxinfo` silently produces nothing (Xwayland down, X auth over ssh) — EGL answers V3D about a path the game never takes, the exact recorded lesson; and `measure_device.sh` pipes all deploy output to `/dev/null` during the one run that mints fps numbers. Each alone is mitigated; together they mean a GLX regression could produce recorded numbers with no warning printed anywhere. **Fixes:** `debug_log_level = info` + grep the engine log for the actual GPU string; device-mode preflight must *require* the GLX renderer line; capture deploy output during measurement and abort the profile on the warning.

**M7 — the config-swap race has a residual gap after SIGKILL.** The `--stop` path re-probes with pgrep and refuses; the deploy path sends `pkill -9`, sleeps 0.5 s, and swaps unconditionally — on a slow SD card an engine blocked in D-state can complete its config write-back *after* the swap, the exact clobber this block exists to prevent. Mirror the `--stop` logic.

**M10 — the parse gate speaks the wrong Lua.** `check_lua.sh` probes `luac` first, which is Homebrew's 5.4: `local a = 1 // 2` passes the gate and fails at load inside the engine's LuaJIT (5.1 syntax). Reorder to `luac5.1 luajit …`.

**M12 *(accepted, unconsidered consequence)* — the floor re-creates the bug the retheme fixed.** `MIN_TREES=4` sampled columns is an expectation of ~64 growths — 3.2× the DoD's 20 — and the next legibility-driven density drop (0.005 ≈ 80 growths, DoD-legal) sits one wobble-column from red. The recorded purpose ("catches a schematic that places nothing") is fully served by `MIN_TREES=1`.

**M13 — the worldgen gate cannot run on the device platform.** `mktemp -d -t h11v-worldgen` is BSD-only; GNU mktemp refuses, `WORK` becomes empty under `set -uo` (no `-e`), and the run collapses into permission errors on `/world`. Same Mac-ism at `deploy_to_term35.sh:177` (L17). Portable form: `mktemp -d "${TMPDIR:-/tmp}/h11v-worldgen.XXXXXX"`.

**M11, M14–M16:** `install_assets.sh --res=16` exits 0 even when the downscaler dies (and `downscale_pack.py` returns 0 on per-file exceptions), because the resolution-detecting verifier legitimately accepts 32 px; `ln -sfn` drops the symlink *inside* a pre-existing real directory (reproduced on this macOS), so `run_local` can silently run a stale copy while `--gameid list` passes; the seam metric compares RGB only — on `h11_bloom` (47% transparent, black under the alpha) the threshold self-inflates to 106.8 vs a measured wrap of 5.8, so its one tiling check is near-vacuous; and a flattened RGB re-export of any "binary" texture passes every check (reproduced with a synthetic file) — the bloom crown would render as solid cubes with a green gate.

**LOW (tools):** L13 `--stop`'s post-KILL probe checks `luanti` but not `minetest` · L14 unit-scale guard string-compares formats sway never emits → redundant reconfigure each launch · L15 clean runs print a mangled error-count line · L16 `measure_device` skips the credential trimming its siblings do · L17 `mktemp -t` BSD-ism in deploy · L18 the free-port probe binds TCP for a UDP port · L19 Ctrl-C runs cleanup twice and prints a misleading FAIL · L20 `assert_min` lets `3-` print ok (status-2 branch) · L21 `--timeout=3m` disables the hang guard entirely · L22 `detect_node_scale` accepts any divisor of 32 — an 8 px tree validates green *(accepted)* · L23 the Lua cross-check regex misses `"a.png^b.png"` composites — v1's planned glyph-overlay pattern · L24 check_assets claims downscale shares its exemption reasoning; it's a separate name list · L25 both PNG decoders treat unknown filter bytes as filter 0 and skip CRCs — corrupt files get classified, not rejected · L26 one malformed PNG aborts the whole `strip_png_metadata` pass.

### D. Design & specification

**H4/H5/H6 — `CLAUDE.md` is the most dangerous file in the repository right now**, because it is injected into every session as overriding instructions. Three independent HIGH findings: (1) "Current state" still declares *specification-only, no code, single commit* — a fresh session could plan v1 greenfield or re-run released phases; (2) the device-constraints paragraph prescribes `video_driver = ogles2` (the 5.10 Irrlicht build rejects it and dies — recorded in decisions.md), font 20 and `gui_scaling ≈ 1.4` (both reintroduce measured, fixed dialog-overflow defects), and a "crosshair interaction style" that does not exist on 5.10; (3) the layout/milestones block references an "M0 spec §7/§8" that no longer exists, claims Luanti is not installed on the Mac, omits `h11_build`, and counts 8 block types. **One rewrite pass fixes all three;** source the values from `tools/device/minetest.conf` and `ARCHITECTURE.md`, which are current.

**H7/H8 — `ARCHITECTURE.md` contradicts the code and itself.** §Components still lists nine nodes under retired ids and three modules (there are 13 and six) — the exact list the doc says v1's mutation rules "are entries over" — while its own §Mutation section already assumes colony-hull susceptibility that §Components doesn't know exists. §Assets still ships "16×16" from `art/h11v/` against two recorded reversals (32×32 verdict; colony pack).

**M17 — ROADMAP v1 was never reconciled with the infection design.** v1.4's DoD demands bounded cycle-*replay*; ARCHITECTURE and CLAUDE.md both settle "never replayed — computed from the frontier in one step". Since `generate-issues` decomposes ROADMAP text, v1 as written would ship the loaded-blocks-only spread the new section forbids. Rewrite v1.2 (frontier selects targets, rules table supplies transformations) and v1.4 (compute-at-load, no replay) **before** running the v1 pipeline.

**M18/M19 — the second model seam is homeless and transportless.** `h11_build` is labelled "(from v2)" in ARCHITECTURE and appears in no roadmap phase; and no document says how a plan reaches the mod — the only defined brain channel is `POST /decide` returning a five-value Intent, and the `Perception → Intent` section still calls itself "the one thing… that may not change", while §The build catalogue calls itself "the second contract". Schedule `h11_build` explicitly; specify the plan transport (a `build` intent carrying a plan reference, or a second endpoint with its own wake condition and budget) and add it to SDLC's seam list.

**M20 *(accepted, unconsidered consequence)* — the anchor and the pure frontier contradict.** The frontier's stated inputs are (pos, cycle, seed); the v4 anchor is promised as "a boundary contact cannot cross" — but contact is explicitly *not* what computes the boundary, so an anchor that acts on the ABM layer cannot stop the frontier, and unloaded blocks behind an anchor would compute `infected=true`. The fix is a design note now: anchor placement/removal history becomes a persistent input to the frontier function.

**M21 — SDLC.md describes `/ship-solution` as "driven from a single issues file… for a bounded piece of work"; the skill is a whole-solution releasing orchestrator.** A contributor with a one-off migration would trigger a releasing pipeline. **M27** — the recorded world-size debt (world still unbounded; `mapgen_limit` appears nowhere) is owed to v1 by decisions.md but appears in no v1 task.

### E. Visualization

**H9 — the ladder order inverted, and three documents certify the opposite.** §4.1 Rule 1 prescribes (top→bottom) *drift > lithic > regolith > hull > prefab > fines* "so the cliffs and dunes read brighter against the ground". The shipped pack measures **hull 86.8 > drift 77.9 > regolith 69.7 > prefab 58.7 > fines 50.4 > lithic 42.0** (independently re-measured by the verifier to 0.1 L\*) — lithic went from second-brightest to darkest, hull to brightest. The *steps* all hold (≥8), which is what §11 checked; the *order* — the part Rule 1 says is chosen deliberately — silently flipped, yet §11 says Rule 1 "holds at every step", ROADMAP v0.8's DoD claims full §4.1 compliance, and `nodes.lua:49` cites §4.1 for lithic being "the darkest rung" — citing the spec for the opposite of what it prescribes. The shipped order is defensible (dark bedrock walls, bright hull); the problem is that a v1.1 biome author following the binding rule will place new materials on wrong L\* bands. **Fix:** amend Rule 1 to the shipped order (or record the deviation in §11 + decisions.md) and correct "holds at every step" to "steps hold; order deliberately changed".

**M22 — two unaudited Rule-2 collisions.** `regolith_side ↔ prefab`: ΔL\* 5.1, ΔH 50°, ΔC only 5.3 — both muted blue-greys with *no chroma lever*, unlike all seven audited pairs (ΔC 26–51). This is the built-against-terrace boundary — the single most common build placement — and at 30% brightness (Rule 6, which §11 never reports) they sit at L\* 17 vs 15 with C\* ≈ 2–4. `lithic_top ↔ fines`: ΔL\* 5.5, ΔH 41°, ΔC 8.3. Neither appears in §11's "seven formal collisions, each checked rather than counted". Adjudicate both: prefab's hue (or its cyan trim's chroma) is the cheap lever; lithic_top↔fines is probably acceptable-with-note.

**M23 — Rule 5 was audited over three pairs; the catalogue ships four.** `nodes.lua:55` wires `h11_lithic_top` as lithic's top face — a pair §11 never measured: **+2.9 L\***, thinner than the +2.6 the same audit called "technically passing, visually thin" and had redrawn for the crate. Brighten `lithic_top` ~5 L\* (headroom to fines at 50.4 exists) or record the acceptance with its reason.

**M24/M25 — the transport bindings have two on-device consequences the v0.7.1 decision never considered.** `gamepad.conf:57` confidently documents `zoom_fov=fov` as the zoom neutralizer — decisions.md records the measured opposite ("did NOT work; turn.lua pins the FOV"), and `zoom_fov` is a player *object property*, not a conf key, so the line is inert; a contributor trusting the comment would delete turn.lua's pin as redundant and reintroduce the flash. And because `creative_mode=true` gives every player the default `zoom_fov` property of 15, **the touch overlay draws a zoom magnifier (~565,160) and a raw `Aux1` text label (~570,295) over the right third of the playfield** — verified in the committed device screenshots — where a tap on the magnifier *turns the camera left* (turn.lua reads `c.zoom` as turn-left) and both zones eat look/dig taps. **Fix:** set the player property `zoom_fov = 0` in `player.lua`'s join handler (kills the button; the keyboard transport still reports through `get_player_control`), investigate suppressing the aux1 overlay, rewrite the gamepad.conf comment, and record the consequence beside the v0.7.1 decision.

**LOW (viz):** L27 the crosshair's cyan tips are the same family as the regolith biofilm specks — it dissolves over pale ground; a 1 px dark underlay fixes it *(accepted art, new consequence)* · L28 default 3D clouds render as huge hard-edged slabs that read as "built" geometry — the silhouette language §2 reserves for the colony · L29 near-white default fog erases the horizon on a bone-pale world at mid range — the v0.8 DoD's "terraces and water readable" fails beyond ~half view range · L30 `docs/device/` screenshots are all pre-retheme yet presented as the current visual record; v0.7's "32×32 reads correctly" verdict was made entirely on v0 textures · L31 the crust/beacon comments say "one step" apart; 12→14 is two.

### F. Cross-cutting (completeness pass, hand-verified where marked)

- **M26 *(hand-verified)*** — `specification/implementation/` carries reports only through v0.3 while git carries six later tags through 0.8.0; `SDLC.md` mandates per-version execution reports and code reviews, and the `harden-findings` skill sweeps exactly those files — so every HIGH/MEDIUM raised since v0.2 is structurally invisible to the phase-boundary harden sweep. Decide and record: either backfill the record or amend SDLC to name this review series as the record.
- **M27** — the world-size debt "owed to v1" has no roadmap home (see table).
- **L32 *(hand-verified)*** — five stray Python venvs sit at the repo root in directories named `#/`, `from/`, `repo/`, `root/`, `the/` — the tail words of `codegen/README.md:34`'s `python3 -m venv .venv    # from the repo root` pasted into interactive zsh (comments disabled by default on macOS). Masked from `git status` by the stock Python `.gitignore`; a directory literally named `#` is a quoting hazard for every future `find`/`rsync`. Delete them; fence the README command.
- **L33** — the shipped audio (`h11_world_music.ogg`, `SilentBeeps.mp3`) has no provenance record, and no gate touches audio; CLAUDE.md's "self-made or CC0" rule is unverifiable for it.
- **L34** — `tools/worldgen_probe/` ships without a `mod.conf`; both engines load it with a deprecation warning today, and the gate that runs for *every* issue is one engine upgrade away from failing for a reason unrelated to the world.
- **L35** — nothing verifies the installed `menu/` art and `screenshot.png` against the delivery (check_assets' default mode covers node textures; `--pack` is broken per H3) — a half-installed menu, the first thing the device user sees, passes every gate.

---

## What's solid

Recorded so the picture is fair — each of these was *checked*, not assumed:

- **The Lua core survives adversarial reading almost untouched.** The schematic's `[z[y[x]]]` construction, prob semantics, alias retirement block, liquid pair plumbing, mapgen override semantics, hand groupcaps (verified against lua_api's dig-time rules for every group/value pair in the catalogue), privilege grant/revoke logic, and music lifecycle all check out against the engine documentation — and nothing in the three player-facing files uses post-5.10 API.
- **`docs/decisions.md` matches the code everywhere the reviewers checked** — the one document that does. The verifier for every doc finding used it as ground truth, successfully.
- **The two new architecture sections stand on primitives that really exist on 5.10** (ABM `without_neighbors` is explicitly tagged 5.10.0; `create_schematic`/`place_schematic`/LBM all present), and the frontier-vs-ABM split is a genuinely correct answer to the loaded-blocks-only problem.
- **The gates' bones are good:** the emerge-then-scan sequence is exactly right against the API; the growths/trees disentanglement is documented at the point of computation with a named STEP constant; `install_assets`' rm-before-rsync hazard *doesn't* fire (existence is checked first); exit-code discipline in check_assets is correct; the staged `.incoming`-then-atomic-mv deploy leaves the device bootable on any failure.
- **The delivered pack is faithfully installed:** installed textures are pixel-identical to the delivery with metadata stripped; `post_effect_color {26,149,113}` matches the measured mean of the shipped water *exactly*; the scanner is a single connected opaque region (zero extrusion crumbs); §11's audit numbers reproduce to 0.1 L\*.

---

## Suggested next actions

1. **Credentials first (H1, ~30 min):** `sshpass -e` in all three deploy paths including `--setup-key`, with a regression check that no `ps`-visible form remains.
2. **One documentation pass (H4–H8, M21, M24, M27):** rewrite CLAUDE.md's three stale sections from `minetest.conf`/ARCHITECTURE, update ARCHITECTURE §Components/§Assets, fix the SDLC `/ship-solution` row, correct gamepad.conf's zoom comment, add the world-size task to v1. This is the highest-leverage batch in the review: five HIGHs die in one commit.
3. **Gate repairs (H2, H3, M10–M14, M16):** each is a few lines; all are "a gate that lies or dies". `run_local.sh`'s `"$@"` and check_assets' `PACK` are one-liners.
4. **Before generating v1 issues (M17–M19):** reconcile ROADMAP v1 with the infection design and give `h11_build` a phase — otherwise the pipeline decomposes a contradiction.
5. **Visualization follow-ups (H9, M22, M23, M25):** one ART-COLONY.md amendment recording the ladder order + the two adjudications + the fourth Rule-5 pair; one `player.lua` line (`zoom_fov = 0`) to kill the mid-screen magnifier. The clouds/fog/crosshair LOWs are worth a single experiment session on the device.
6. **The LOW backlog** at leisure — none of it blocks v1.

A follow-up `/review-and-fix-issues` pass can work straight from the table above; this document is written to be updated in place (add a Status column entry per finding as fixes land), per the project's review convention.
