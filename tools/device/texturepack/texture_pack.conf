name = H11V device overrides
description = Blanks the on-screen jump and sneak buttons, which Luanti 5.10 draws across the hotbar with no way to move or disable them. The PocketTerm35 has a physical keyboard, so both actions already have keys.
